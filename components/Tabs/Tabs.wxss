.tabs .tabs_title {
  display: flex;
  padding: 15rpx 0;
}
.tabs .tabs_title .titel_item {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 15rpx 0;
}
.tabs .tabs_title .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
